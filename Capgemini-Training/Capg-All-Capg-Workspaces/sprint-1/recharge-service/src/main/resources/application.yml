server:
  port: 8086

spring:
  application:
    name: recharge-service

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    driver-class-name: oracle.jdbc.OracleDriver   # ✅ FIXED
    username: scott
    password: satakratu789

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true   # 🔥 MUST ADD

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000