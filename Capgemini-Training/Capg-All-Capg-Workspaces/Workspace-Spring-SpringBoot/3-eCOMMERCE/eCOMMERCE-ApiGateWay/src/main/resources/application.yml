server:
  port: 9080   #  

spring:
  application:
    name: API-GATEWAY

  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://ORDER-SERVICE   # SERVICE NAME, NOT PORT
          predicates:
            - Path=/order/**