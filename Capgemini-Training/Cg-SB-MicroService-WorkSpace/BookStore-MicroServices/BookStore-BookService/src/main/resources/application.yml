server:
  port: 9082   # Book Pvice Port

spring:
  application:
    name: BOOK-SERVICE

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: scott
    password: satakratu789
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update   # IMPORTANT (read below)
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka